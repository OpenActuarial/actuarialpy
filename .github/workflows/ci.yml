name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: OpenActuarial/.github/.github/workflows/reusable-ci.yml@v1
    with:
      package: actuarialpy
      coverage-min: 86
      extra-pip: "chainladder"
